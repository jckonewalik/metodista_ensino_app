const INITIAL_STATE = {
  currentClass: null,
};

const classReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default classReducer;
